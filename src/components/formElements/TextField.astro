---
const { label, name, type = "text", value = "", placeholder = "", required = false } = Astro.props;
---
<div class="form-field">
  <label for={name}><strong>{label}</strong></label>
  <br />
  <input type={type} name={name} id={name} value={value} placeholder={placeholder} required={required} />
</div>