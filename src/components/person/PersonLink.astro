---
const { name, pid } = Astro.props;
const displayName = name?.trim() ? name : "Not recorded";
---

{pid ? (
    <a href={`../person?pid=${pid}`}>{displayName}</a>
) : (
    <span>{displayName}</span>
)}